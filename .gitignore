*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
# Per-client installs are runtime COPIES that live inside the master htdocs
# (served by the wildcard dispatcher). They must never be tracked, or git
# operations (pull/reset) collide with their 10k+ files → merge conflicts.
/*.castlitpos.com/
# LWS control-panel default page + per-install deploy version stamp.
/default_index.html
/.version
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
